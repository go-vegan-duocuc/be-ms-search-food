version: '3.9'

services:
  ms-search-food:
    build:
      context: ./ms-search-food
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - SPRING_PROFILES_ACTIVE=prod
    networks:
      - my-network

networks:
  my-network:
    driver: bridge

# docker-compose build --no-cache
# docker-compose up
